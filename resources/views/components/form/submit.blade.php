<div class="text-right my-3">
    <button type="submit"
        class="font-bold text-lg rounded-full text-sm px-10 py-3 {{ theme("bg-blue", "text-white") }}">
        <i class="{{ $icon ?? "fas fa-paper-plane" }} mr-3"></i>
        {{ __($text) }}</button>
</div>